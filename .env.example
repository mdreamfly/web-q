# SearXNG secret key (generate with: openssl rand -hex 32)
SEARXNG_SECRET=your-secret-key-here

# ============================================================
# LLM Provider Configuration
# ============================================================
# Choose your LLM provider: "openai", "openrouter", or "custom"
LLM_PROVIDER=openrouter

# ------------------------------------------------------------
# OpenRouter (default) - access to many models at low cost
# Get key at: https://openrouter.ai/keys
# ------------------------------------------------------------
OPENROUTER_API_KEY=your-openrouter-key-here
OPENROUTER_MODEL=google/gemini-2.0-flash-lite-001
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ------------------------------------------------------------
# OpenAI - use official OpenAI models (GPT-4o, GPT-4o-mini, etc.)
# Get key at: https://platform.openai.com/api-keys
# ------------------------------------------------------------
# OPENAI_API_KEY=sk-your-openai-key-here
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# ------------------------------------------------------------
# Custom Provider - any OpenAI-compatible API
# Examples: Ollama (http://host.docker.internal:11434/v1),
#           vLLM, LiteLLM, Azure OpenAI, etc.
# ------------------------------------------------------------
# CUSTOM_API_KEY=your-custom-key-here
# CUSTOM_MODEL=your-model-name
# CUSTOM_BASE_URL=http://host.docker.internal:11434/v1

# Compression system prompt (optional, has sensible default)
# COMPRESSION_PROMPT=Process the following content according to the user's instruction. Preserve key facts, names, numbers, and actionable information. Output only the result, no preamble.
